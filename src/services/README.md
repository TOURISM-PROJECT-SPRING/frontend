# Services Layer

Business logic abstraction over API calls. Components call services, not API directly.

- `destinationService.js` - Destination business logic
- `bookingService.js` - Booking business logic
- `authService.js` - Auth business logic (login, register, logout)
- `userService.js` - User profile business logic
