import Icon from "../ui/Icon";

// SovannDomNour-style award badge that sits bottom-left over the photo.
// Uses the brand green + a laurel/award mark rather than any third-party logo.
export default function AwardBadge({ award }) {
  if (!award) return null;
  const best = award === "best";
  return (
    <span>
    </span>
  );
}
